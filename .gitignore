node_modules/
*.png
# ...exceto os ícones do PWA (PNG é asset deployável; ver PLANO-pwa.md / PROMPT-icone.md)
!louvai-icons/*.png
*.log
# cópia pronta pra subir no GitHub Pages (regenerável: cp louvai.html index.html)
index.html
# mensagem de commit temporária (usada p/ evitar problemas de aspas no PowerShell)
COMMIT_MSG_tmp.txt
# backups locais do louvai.json VIVO (baixados do remoto a cada deploy) — histórico de dados
# da equipe na máquina; não versionar (o remoto já é a fonte da verdade). Ver scripts/publish.sh.
backups/
