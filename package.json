{
  "name": "louvai",
  "version": "0.15.1",
  "private": true,
  "type": "module",
  "description": "App de cifras offline-first para ministério de música (arquivo único HTML).",
  "scripts": {
    "test": "node tests/smoke.mjs",
    "test:install": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
